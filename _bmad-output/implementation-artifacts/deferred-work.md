# Deferred Work

## Deferred from: code review of 3-5-relance-automatique-en-cas-de-non-lecture-dun-message-prioritaire (2026-07-16)

- Fenêtre résiduelle très étroite : si l'update de réclamation (`relance_envoyee_at`, `lib/relance.ts`) réussit mais que l'annulation qui suit un échec Telegram (remise à `null`) échoue *elle aussi* (double panne Supabase quasi simultanée), la Conversation reste marquée "relancée" sans qu'aucune notification n'ait réellement été délivrée — reproduit alors, dans une fenêtre beaucoup plus étroite qu'avant la correction, le risque initial de perte silencieuse identifié en revue de code. **Décision (2026-07-16)** : risque accepté, jugé négligeable (deux échecs Supabase quasi simultanés sur la même ligne) — aucune action pour l'instant ; à revisiter si les logs Vercel montrent que ce chemin se produit en pratique.
- Race théorique entre l'écriture différée (`after()`) de `last_organizer_read_at` dans `marquerLu` (`app/organisateurs/actions.ts`) et la lecture de ce même champ par le cron de relance — fenêtre de quelques dizaines de millisecondes, une fois par jour, impact borné à une notification Telegram redondante mais inoffensive (l'organisateur a déjà la conversation ouverte). **Décision (2026-07-16)** : risque accepté, probabilité et impact jugés négligeables — aucune action.
- Regroupement des updates Supabase par lot (`.in("id", ...)`) au lieu d'un update par Conversation éligible, proposé en revue de code comme optimisation : **non appliqué**, devenu incompatible avec la réclamation atomique par conversation (chaque ligne a sa propre condition `.or()` dépendant de son propre dernier message élève, donc pas trivialement regroupable en un seul appel) — la correction de sécurité (réclamation atomique) prime sur cette optimisation mineure à cette échelle (2 organisateurs, faible volume).

## Deferred from: implémentation de 3-5-relance-automatique-en-cas-de-non-lecture-dun-message-prioritaire (2026-07-16)

- `CRON_SECRET` n'a été ajouté qu'en local (`.env.local`) — l'ajout dans les Environment Variables du projet Vercel (Production) a été **refusé par le mode automatique de l'agent** (écriture de secret en production jugée trop sensible pour une action non explicitement demandée par Charles). Commande prête à lancer par Charles : `vercel env add CRON_SECRET production` (valeur déjà générée, voir `.env.local`). Tant que ce n'est pas fait, la route `/api/cron/relance` refusera (401, fail-closed voulu) tout déclenchement réel par Vercel Cron en production — comportement sûr par défaut, mais la relance FR-15 restera inactive en prod jusqu'à cette étape.
- `vercel.json` (déclaration du cron quotidien) créé dans cette session mais **pas encore déployé** — ne prend effet qu'après un push sur `main` (AD-10). Tant que ce n'est pas fait, aucun déclenchement automatique n'existe, même une fois `CRON_SECRET` configuré côté Vercel.
- Vérification de bout en bout limitée au niveau code (route + logique d'éligibilité, testées via `curl` local avec données Supabase backdatées) — le déclenchement réel par l'infrastructure Vercel Cron en production ne pourra être confirmé qu'après déploiement et `CRON_SECRET` renseigné (voir ci-dessus), en observant les logs Vercel (section Cron Jobs du dashboard).

## Deferred from: implémentation de 3-4-notification-instantanee-sur-nouveau-message-et-notification-double-en-cas-de-priorite (2026-07-16)

- `TELEGRAM_CHAT_IDS` ne contient pour l'instant que le `chat_id` de Charles (`6721003957`), aussi bien en local que sur Vercel (Production) — celui de Basile n'a pas pu être obtenu dans cette session (il doit lui-même envoyer un message au bot `@laparoleavanttout_bot`, puis Charles relit `getUpdates` pour récupérer son `chat.id`). Tant que ce n'est pas fait, un seul Organisateur reçoit les notifications, ce qui contredit partiellement FR-10 ("jamais un seul face à la décision d'escalade") en cas de Signal de danger réel. À compléter avant un usage en production (mettre à jour la variable `TELEGRAM_CHAT_IDS` en local **et** sur Vercel, `vercel env rm` puis `vercel env add`).
- ~~Variables Vercel (Production) pas configurées~~ — fait le 2026-07-16 (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_IDS` ajoutées via `vercel env add`, confirmées par `vercel env ls`). Redéploiement encore nécessaire pour qu'elles soient effectives (prochain push sur `main`, ou `vercel --prod` manuel) — pas fait automatiquement par l'ajout d'une variable.
- Piège rencontré pendant la vérification, à garder en tête pour la Story 3.5 (relance automatique, même mécanisme Telegram) : dans la réponse JSON de `getUpdates`, `update_id` et `chat.id` sont deux nombres différents qui apparaissent l'un après l'autre — facile à confondre pour un premier essai (vécu par Charles sur cette story, corrigé après un test en échec `"chat not found"`).

## Deferred from: code review of 3-2-consultation-des-conversations.md / 3-3-reponse-a-une-conversation.md (2026-07-16)

- Aucune identité d'auteur sur les messages Organisateur (`messages.sender_type = 'organisateur'` sans `organizer_id`/équivalent) — impossible de savoir lequel des deux Organisateurs a répondu ou consulté une Conversation donnée. **Décision de Charles (2026-07-16)** : conservé tel quel, boîte de réception partagée assumée entre les deux Organisateurs, cohérent avec l'absence d'assignation de Conversation déjà actée (AD-8, FR-6) — aucune action pour l'instant.
- `ReplyForm` (`app/organisateurs/[conversationId]/reply-form.tsx`) désactive le bouton d'envoi pendant `isPending` mais laisse le `textarea` éditable, sans garde anti-double-soumission rapide (l'utilisateur peut cliquer/valider une seconde fois avant le re-render React qui désactive le bouton) — même comportement déjà présent dans `message-form.tsx` (Story 1.3), pas une régression introduite par la Story 3.3. Pire cas : une réponse dupliquée envoyée à l'élève. À corriger dans les deux formulaires en même temps si jugé prioritaire.
- `formData.get(...)?.toString()` n'est vérifié comme étant réellement une chaîne (`typeof === "string"`) nulle part dans le projet avant cette revue — présent dans `envoyerMessage`/`choisirModeSauvegarder` (`app/discussion-anonyme/actions.ts`, Stories 1.2/1.3) en plus du nouveau `repondre()` (Story 3.3, corrigé lors de cette revue). Une requête POST forgée en dehors du formulaire réel (avec un champ `File` au lieu d'un texte) insérerait le texte littéral `"[object File]"` en base sur les deux sites non corrigés. Risque faible (pas d'injection, juste un message-poubelle), regroupé ici pour un futur passage de durcissement groupé plutôt que trois corrections séparées.

## Deferred from: code review of 3-2-consultation-des-conversations (2026-07-15)

- Les requêtes `conversations` et `messages` (`app/organisateurs/page.tsx`) n'ont ni tri explicite ni pagination — soumises à la limite par défaut de Supabase/PostgREST (1000 lignes), qui pourrait un jour tronquer silencieusement la liste ou fausser le calcul de "dernier message élève". Accepté à l'échelle actuelle (NFR-1, AD-10), à revisiter si le volume de Conversations/messages augmente significativement. Complément (revue #2) : le filtre `.in("conversation_id", ...)` de la requête `messages` encode tous les ids dans l'URL — au-delà de quelques centaines de Conversations, risque de limite de longueur d'URL côté proxy/CDN, mode d'échec distinct à traiter en même temps que la pagination.
- Aucune validation runtime que la réponse Supabase correspond aux types TypeScript déclarés (`ConversationRow`/`MessageRow`) — pattern déjà présent partout ailleurs dans le projet (ex. `app/discussion-anonyme/page.tsx`), pas introduit par cette story. Complément (revue #2) : même principe pour les timestamps — `Date.parse`/`Intl.DateTimeFormat` supposent un `timestamptz` toujours bien formé (garanti par Postgres/PostgREST sur tous les chemins de code actuels), sans garde-fou explicite si cette garantie était un jour rompue.

## Deferred from: code review of 2-3-procedure-descalade-humaine-documentee (2026-07-10)

- Signal de danger concernant un tiers (pas l'Élève lui-même) non couvert par la procédure d'escalade (`docs/procedure-escalade-cpe-counsellor.md`) — FR-9 définit le Signal de danger uniquement autour des idées suicidaires/de l'automutilation de l'Élève lui-même ; un message décrivant un danger venant d'un tiers (ex. maltraitance en cours) est une classe d'escalade différente non couverte. Extension de périmètre à trancher au niveau produit (FR-9), pas dans cette story.
- Aucune boucle de confirmation/clôture avec la CPE après l'appel d'escalade — orthogonal au cœur des AC de cette story (informer la CPE), question de process plus large non bloquante pour l'usage immédiat de la procédure.

## Deferred from: code review of 1-1-mise-en-place-du-projet-et-divulgation-de-confidentialite (2026-07-08)

- `app/page.tsx` (racine "/") reste le boilerplate par défaut de `create-next-app` (liens sortants Vercel/Next.js). Déjà noté dans le File List de la story comme "pas encore celui d'Epic 4" ; propriété de la Story 4.1 (point d'entrée vers le chat).
- `messages.body` n'a pas de contrainte anti-vide (`not null` accepte `''`). La validation du contenu d'un message relève de la Story 1.3 (envoi du premier message et accusé de réception).
- Historique de migrations de la CLI Supabase désynchronisé de la base réelle (migration appliquée à la main via SQL Editor suite à l'échec de `supabase db push` — bug connu du pooler Supavisor). Le SQL est idempotent (`if not exists`) donc sans risque immédiat, mais à réconcilier avant la prochaine migration (Story 1.5).

## Deferred from: code review of 1-2-choix-du-mode-de-conversation-sauvegarder-ou-ephemere (2026-07-08)

- Race condition TOCTOU sur l'unicité du Code : `isCodeAvailable` et l'`insert` dans `choisirModeSauvegarder` ne sont pas atomiques, et le sel bcrypt empêche toute contrainte d'unicité en base. Deux élèves soumettant le même Code en même temps peuvent tous les deux réussir. **Décision de Charles (2026-07-08)** : risque accepté, jugé improbable à l'échelle d'un lycée — aucune action pour l'instant.
- Le paramètre `mode=ephemere` est posé sur la redirection de `choisirModeEphemere` mais jamais lu dans `page.tsx` — soit à exploiter (message spécifique au mode éphémère sur l'écran "prêt"), soit à supprimer.
- Visiter `?conv=<id>` sans `?etape=pret` retombe silencieusement sur l'écran de choix de mode plutôt que de gérer explicitement cet état partiel — non bloquant, aucune AC ne le couvre.
- Aucune limitation de débit sur `choisirModeSauvegarder`, qui déclenche une boucle `bcrypt.compare` en O(n) à chaque soumission. Le O(n) lui-même est un compromis d'architecture assumé (Dev Notes/NFR-1), mais l'absence de limitation de débit reste un vecteur d'épuisement CPU distinct — à traiter dans un futur passage de durcissement.

## Deferred from: code review of 1-3-envoi-du-premier-message-et-accuse-de-reception (2026-07-09)

- Le Code de récupération est mis en minuscules dans `choisirModeSauvegarder` (`app/discussion-anonyme/actions.ts:44`) sans que `mode-choice.tsx` n'avertisse l'élève que son Code est insensible à la casse. Code/copie de la Story 1.2, non touchés par le diff de la Story 1.3 — repéré seulement maintenant.
- (Pour mémoire, pas une nouvelle entrée) La race TOCTOU sur l'unicité du Code, le scan O(n) `bcrypt.compare` et l'absence de limitation de débit ont été re-signalés par cette revue mais sont déjà couverts par les entrées ci-dessus (revue de la Story 1.2, 2026-07-08) — décision de Charles déjà actée, rien de nouveau à trancher.

## Deferred from: code review of 1-5-recuperation-dune-conversation-via-code (2026-07-09)

- `recovery-form.tsx` duplique intégralement le balisage des formulaires `mode-choice.tsx`/`message-form.tsx` (wrapper, label/input, message d'erreur, bouton) sans composant partagé — cohérent avec la préférence déjà documentée de ce dépôt pour la duplication à cette échelle (NFR-1) plutôt qu'une abstraction prématurée, comme déjà tranché pour la triplication du scan bcrypt dans `lib/session.ts` (Story 1.4/1.5). Aucune action pour l'instant.
- Verrou anti-brute-force (`recovery_attempts`) contournable par requêtes concurrentes (TOCTOU) : `isRecoveryLocked` et `recordRecoveryAttempt` ne sont pas atomiques. **Décision de Charles (2026-07-09)** : risque accepté, même position que la race TOCTOU sur l'unicité du Code (Story 1.2) — aucune action pour l'instant.
- Le verrouillage anti-brute-force échoue "ouvert" (fail-open) sur toute erreur Supabase transitoire, désactivant temporairement la protection. **Décision de Charles (2026-07-09)** : conservé tel quel, cohérent avec le reste du code (NFR-2) — aucune action pour l'instant.
- Le verrou anti-brute-force est partagé par IP brute : des élèves derrière la même IP (Wi-Fi/NAT du lycée) partagent le même compteur de tentatives. **Décision de Charles (2026-07-09)** : risque accepté, situation jugée rare — aucune action pour l'instant.
- Récupérer une conversation via Code sur un nouvel appareil invalide silencieusement le cookie de tout autre appareil déjà connecté à cette conversation, sans message d'erreur affiché sur cet autre appareil. **Décision de Charles (2026-07-09)** : laissé silencieux, cas jugé rare — aucune action pour l'instant.
